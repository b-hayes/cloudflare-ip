# Cloudflare API Configuration
# Copy this file to .env and fill in your values

# For single site usage: Fill in all values below
# For multiple sites: Use this for shared settings, create site-specific .env files for overrides

# Required: Your Cloudflare account email
CLOUDFLARE_EMAIL=your_email@example.com

# Required: Your Cloudflare Global API Key (found at https://dash.cloudflare.com/profile/api-tokens)
CLOUDFLARE_API_KEY=your_global_api_key_here

# Required: Your domain's zone ID (found in Cloudflare dashboard -> domain -> right sidebar)
CLOUDFLARE_ZONE_ID=your_zone_id_here

# Required: The DNS record name to update (e.g. example.com or subdomain.example.com)
CLOUDFLARE_RECORD_NAME=example.com

# Optional: DNS record type (default: A)
CLOUDFLARE_RECORD_TYPE=A

# Optional: TTL in seconds (default: 300 = 5 minutes)
CLOUDFLARE_TTL=300